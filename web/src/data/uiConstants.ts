/**
 * UI Constants and Magic Numbers
 */

export const UI_CONSTANTS = {
    // Scroll threshold for manual back-to-top button visibility
    BACK_TO_TOP_THRESHOLD: 500,

    // Responsive breakpoints (matching CSS)
    BREAKPOINTS: {
        MOBILE: 768,
        TABLET: 900,
        DESKTOP_LR: 1500,
    }
};
